- flash.each do |key, msg|
  .alert.alert-success
    %a.close{"data-dismiss" => "alert"}×
    = content_tag :div, msg, :class => [key, " message"], :id => "notice_#{key}"
    = content_tag :script, :type => "text/javascript" do
      setTimeout(function() { $('.alert-success').fadeOut(1600); }, 5000);
